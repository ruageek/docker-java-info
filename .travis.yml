language: java
sudo: false
install: true

jdk:
  - openjdk6
  - openjdk7
  - openjdk8
  - oraclejdk8
  - oraclejdk9

cache:
  directories:
    - "${HOME}/.m2/repository"
    - "${HOME}/.sonar/cache"

notifications:
  email: true
